{
  "rules": [
    {
      "allowed": {
        "origins": ["*"],
        "methods": ["GET", "HEAD"],
        "headers": ["If-None-Match", "If-Modified-Since"]
      },
      "exposeHeaders": ["ETag", "Last-Modified", "Cache-Control"],
      "maxAgeSeconds": 86400
    }
  ]
}
